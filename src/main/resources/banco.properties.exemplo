# Dados de conexao com o PostgreSQL.
# Se precisar mudar de maquina, so alterar aqui (ou exportar as variaveis
# de ambiente PROVEDOR_DB_URL / PROVEDOR_DB_USER / PROVEDOR_DB_PASSWORD).
db.url=jdbc:postgresql://localhost:5432/provedor_db
db.usuario=postgres
db.senha=postgres
